#include <bits/stdc++.h>
using namespace std;

string s;

int main(){
    
    for(int i = 0 ; i < 100 ; i++){
        getline(cin, s);       
        cout<<s<<'\n';       
    }

    for (char a[99]; gets(a);)
        puts(a);                //*get,put
}