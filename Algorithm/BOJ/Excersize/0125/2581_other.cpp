#import <iostream>
int n, m, s, i, r;
main()
{
    for (std::cin >> n >> m; n <= m; n - i ?: (r ?: r = n, s += n), n++)
        for (i = 2; i < n && n % i++;)
            ;
    printf(r ? "%d %d" : "-1", s, r);
}